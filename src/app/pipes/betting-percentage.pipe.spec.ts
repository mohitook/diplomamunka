/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BettingPercentagePipe } from './betting-percentage.pipe';

describe('Pipe: BettingPercentagee', () => {
  it('create an instance', () => {
    let pipe = new BettingPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});